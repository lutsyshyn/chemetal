<%= link_to 'Back to Journal', journal_path(@abstract.article.journal) %><br/>


<%= render 'shared/title', article: @abstract.article %>

<%= render 'shared/authors', article: @abstract.article %>

<div class="abstract_content">
  <%= @abstract.content.html_safe %>
</div>

<div class="abstract_keywords">
  Keywords:<br/>
  <%= @abstract.keywords %>
</div>

